The contents of this directory (`o2`) are self-contained and
do not depend on the TACE codebase. 

They can be directly copied and integrated into other models.

However, compatibility is not currently guaranteed.

If you use the local O(2) method, please cite our paper.
