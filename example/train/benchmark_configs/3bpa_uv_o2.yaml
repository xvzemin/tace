# Use the 3BPA CGTP training setup with a local O(2) interaction.
defaults:
  - 3bpa_cgtp@_global_
  - _self_

model:
  config:
    mmax: 3
    atomic_basis:
      type: o2
      edge_nonlinear: gate
      scalar_act: [silu, tanh]
